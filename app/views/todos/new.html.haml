%h1 Todo登録フォーム
= form_for @todo do |f|
    - if @todo.errors.any?
        %ul
            - @todo.errors.full_messages.each do |msg|
                %li
                    #{msg}
    %table
        %tr
            %td
                = f.label :title, "タイトル"
            %td
                = f.text_field :title
        %tr
            %td
                = f.label :body, "本文"
            %td
                = f.text_field :body
        %tr
            %td
                = f.submit "Todoを登録する"
                = link_to "一覧に戻る",todos_path

